{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Configure xwiki",
    "$id": "http://nethserver.org/json-schema/task/input/xwiki/configure-module",
    "description": "Configure xwiki",
    "examples": [
        {
            "host": "xwiki.domain.org",
            "http2https": true,
            "lets_encrypt": true,
            "java_heap_mb": 1024
        }
    ],
    "type": "object",
    "required": [
        "host"
    ],
    "properties": {
        "host": {
            "type": "string",
            "description": "Host name for the application, like 'xwiki.domain.org'",
            "format": "hostname",
            "pattern": "\\."
        },
        "lets_encrypt": {
            "type": "boolean",
            "title": "Let's Encrypt certificate",
            "description": "Request a valid Let's Encrypt certificate."
        },
        "http2https": {
            "type": "boolean",
            "title": "HTTP to HTTPS redirection",
            "description": "Redirect all the HTTP requests to HTTPS"
        },
        "java_heap_mb": {
            "type": "integer",
            "title": "JVM heap size (MB)",
            "description": "XWiki Tomcat maximum heap memory in megabytes",
            "minimum": 1024,
            "maximum": 8192,
            "default": 1024
        }
    }
}
